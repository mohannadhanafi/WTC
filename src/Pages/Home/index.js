import React from 'react';
import { useSelector } from 'react-redux';

const Home = () => {
  // useSelector((state) => console.log('stateb ===> ', state.userData.userData));
  return (
    <div>
      <h1>Home</h1>
    </div>
  );
};

export default Home;
